services:
  - type: web
    name: phone-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.backend:app --host 0.0.0.0 --port 8000
    autoDeploy: true
    # keep it warm: hit the health path every 10 min
    healthCheckPath: /
